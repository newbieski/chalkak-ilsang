# SERVICE · 찰칵일상 (Chalkak-ilsang)

## 1. 사용자·문제·가치
- 누구를 위한 서비스인가: 개인 사용자 (본인 사진첩을 정리하고 싶은 사람)
- 어떤 문제를 푸는가: 사진에 어울리는 설명 글을 그때그때 직접 써야 하는 번거로움 + 사진을 기준별로 정리·검색하기 어려움 (핵심은 글쓰기 쪽)
- 지금은 어떻게 해결하나: 사진을 보면서 그때그때 직접 글을 쓰고, 폴더로 대충 분류하며, 나머지는 스크롤과 기억에 의존
- 이걸로 뭐가 좋아지나: 사진을 올려두면 원하는 톤·분량으로 설명 글을 자동으로 받고, 자동 태그로 자연어·필터 검색까지 가능해진다

## 2. 서비스 확장 관점
- 기여: 개인의 사진 정리·기록 시간을 줄이고, 사진을 다시 찾아보고 싶게 만든다
- 규모: 개인 소장 사진 수백~수천 장
- 대체제와 차별점: 구글·애플 사진 등은 자동 태그·검색은 이미 제공하지만, 사람이 읽을 만한 문장형 설명(그것도 사용자 톤에 맞춘)은 만들어주지 않는다 — 이게 핵심 차별점
- 향후 확장: 지금은 사진이 이미 저장소에 있다는 전제로 시작하지만, 이후에는 사용자가 직접 사진을 올리는 업로드 기능까지 확장할 계획이다 (이번 범위에서는 제외)

## 3. 사용 예상 도구·데이터
- 도구 (에이전트가 직접 호출하는 4개)
  - `index_photos`: 저장소에 들어온 사진에 일괄로 기본 태그를 붙인다. 이미지 내용 + EXIF(있는 경우)를 근거로 사물·장면·촬영 맥락 태그를 생성해 `data/photos.json` 에 기록한다
  - `generate_caption`: 사용자가 고른 사진의 설명 글을 만든다. 톤(프리셋 또는 글 샘플 기반 커스텀 톤)과 분량(한 줄~5문장), 사용자가 준 가이드 문장을 반영한다
  - `search_photos`: 자연어 질문 검색과 태그·기간 필터 검색을 모두 처리한다
  - `get_photo`: 사진 한 장의 태그·촬영정보·기존 캡션을 상세 조회한다
  - (사용자가 생성된 문장·태그를 고쳐 저장하는 동작은 에이전트 도구가 아니라 앱 기능으로 둔다)
- 데이터
  - 사진: 실제 이미지 파일 (라이선스 문제 없는 인터넷 샘플, 인물 사진 포함) 최대 30장 내외. 사진 원본 파일은 로컬 테스트 전용이라 저장소에는 올리지 않고, 메타데이터(JSON)만 커밋한다. 사진은 이미 저장소에 올라와 있다는 전제로 시작하고, 업로드 기능 자체는 이후 단계에서 별도 구현
  - `data/photos.json`: 사진별 촬영정보(EXIF 날짜·위치, 없는 사진도 섞음)와 인덱싱으로 채워지는 태그·캡션을 보관한다. 실행을 껐다 켜도 남아 검색에 쓰인다
  - `data/tones.json`: 톤 프리셋 목록과 사용자 커스텀 톤의 글 샘플을 함께 보관한다

## 4. 서비스 정책 (가드레일 요약)
- 등록되지 않았거나 존재하지 않는 사진에 대해 지어내지 않는다
- 설명 생성 자체는 자유롭게 하되, 외부 공유·내보내기 시점에 사생활 정보 포함 여부를 경고하고, 사용자가 상황별로 정책을 조정할 수 있게 한다
- 사용자가 지정하지 않은 톤·분량을 임의로 바꾸지 않는다
- 태그·캡션 어디에도 실제로 확인되지 않은 사실은 넣지 않는다 — 메타데이터가 존재하더라도 그 값 자체가 근거 없이 채워진 것일 수 있으므로, "메타데이터가 있다"는 이유만으로 사실처럼 반영하지 않는다 (사물·장면처럼 이미지에서 직접 확인 가능한 것만 확정적으로 말한다)

## 5. 응답 형식 (API 계약)
- 요청: POST /query, question 필드로 자연어 질문을 받는다
- 응답
  - answer:
  - contexts:
  - trace:

## 6. 예외 상황과 처리
- 등록되지 않았거나 존재하지 않는 사진을 요청받으면: 지어내지 않고 "해당 사진을 찾을 수 없다"고 답한다
- EXIF(날짜·위치)가 없는 사진: 촬영 맥락은 "알 수 없음"으로 두고, 이미지 내용만으로 태그·설명을 생성한다 (날짜·위치를 추측해서 채우지 않는다)
- 이미지 화질이 낮거나 내용이 불분명해 태그 신뢰도가 낮은 경우: 해당 태그를 "추정" 표시로 남기고 자동 확정하지 않는다 — 사용자가 확인·수정할 수 있게 한다
- 사용자가 톤을 지정하지 않은 경우: 기본 톤("담백한 사실 설명")을 사용하고, 임의로 다른 톤을 추측해 적용하지 않는다
- 내보내기·공유 시 사생활 민감 요소(인물 얼굴, GPS 좌표, 문서·카드 등 텍스트가 보이는 사진 등)가 감지되면: 생성 자체는 막지 않되 공유 직전에 경고를 띄우고 사용자가 항목별로 포함 여부를 선택하게 한다
- 자연어 검색 질의가 모호하거나 태그 체계와 맞지 않는 경우: 임의로 가장 비슷한 결과를 확정해서 보여주지 않고, 해석 가능한 후보(태그·기간 등)를 제시해 사용자가 좁히게 하거나 "해당하는 사진 없음"으로 응답한다
- 캡션 생성이 실패·타임아웃되는 경우: 원본 사진·기존 메타데이터는 보존한 채 실패를 알리고 재시도할 수 있게 한다 (부분 결과를 사실인 것처럼 채워 넣지 않는다)

## 7. 사용 시나리오 예시
- 여행 사진 일괄 정리: 작년 여름 제주도 여행 사진 50장이 이미 등록돼 있는 상태에서, 사용자가 "담백한 사실 설명, 2문장"으로 일괄 캡션 생성을 요청 → 사진마다 장소·사물·장면 태그와 설명이 붙음 → 이후 "작년 여름 제주도에서 찍은 바다 사진 보여줘"라고 자연어로 검색해 바로 찾는다
- 커스텀 톤 적용: 사용자가 자신이 평소 쓰는 글 몇 개를 톤 샘플로 등록 → 이후 캡션 생성 시 그 톤으로 재생성 요청 → 같은 사진이라도 기본 톤과 다른 문장으로 결과를 받는다
- 공유 전 경고: 사용자가 캡션이 달린 사진 한 장을 외부로 내보내려는데, 그 사진에 GPS 좌표와 지인 얼굴이 포함돼 있음 → 내보내기 직전 시스템이 "위치 정보·인물이 포함되어 있습니다"라고 경고 → 사용자가 위치 정보만 제외하고 내보내기를 선택
- 존재하지 않는 사진 질의: 사용자가 "작년 겨울 스키장 사진 보여줘"라고 검색했지만 등록된 사진 중 스키장 관련 사진이 없음 → 시스템이 없는 사진을 지어내지 않고 "해당하는 사진을 찾지 못했다"고 답한다

## 8. 성공 기준
- 정성: 사용자가 예전 사진을 다시 찾아볼 때 "이 사진이 뭐였지" 하며 스크롤·기억에 의존하는 시간이 눈에 띄게 줄어든다
- 자동 생성된 캡션을 사용자가 수정 없이 그대로 채택하는 비율이 일정 수준 이상이다 (수정이 잦으면 톤·분량 반영이 안 되고 있다는 신호)
- 자연어 검색으로 원하는 사진을 찾을 때 질의를 다시 쓰는 횟수(재질의 횟수)가 적다
- `evaluation/test_queries.csv` 인-아웃 세트 20건 중 최소 80%(16건) 이상 통과해야 완성으로 본다
- guardrail 카테고리는 예외 없이 전건 통과(위반 0건)를 별도 기준으로 둔다 — 존재하지 않는 사진에 대한 캡션·태그 생성, 사용자 미지정 톤·분량 임의 변경이 포함된다

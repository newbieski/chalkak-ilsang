# 진행 기록

과제 규약은 [MINIPJT.md](MINIPJT.md), 서비스 정의는 [SERVICE.md](SERVICE.md) 참고.

## Day 8 · 2026-09-16 · 킥오프·기획

### 한 일
- SERVICE.md 6~8번(예외 상황·사용 시나리오·성공 기준) 작성해 문서 완성
- 강의 규약 원문 확인 후 과제 로지스틱을 MINIPJT.md로 분리 기록
- `evaluation/test_queries.csv` 인-아웃 세트 20건 작성 (positive 8 · negative 4 · edge 5 · guardrail 3)
- 인터뷰 방식으로 데이터·도구 스펙을 좁혀 SERVICE.md 3번 갱신
- `data/photos.json`(사진 10건) · `data/tones.json`(톤 프리셋 3종 + 커스텀 톤) 작성

### 결정한 것과 이유
- **"서비스 스펙"은 SERVICE.md 작성이 아니라 데이터·도구 준비를 가리킨다.** 일정표 블록 제목이 "기획 마무리 + 인-아웃 세트 + 서비스 스펙" 세 갈래이고 불릿도 셋이라 1:1 대응시킴. 처음엔 SERVICE.md 작성으로 잘못 읽었다가 정정.
- **캡션은 모델이 이미지를 직접 보고 생성한다** (태그·EXIF는 보조 근거). 그 결과 "더미 메타데이터만 있는 사진"이라는 개념이 성립하지 않아 SERVICE.md의 해당 문구를 실제 이미지 10장 기준으로 고쳤다.
- **태그는 저장 시점에 일괄로 붙인다.** 사용자가 사진마다 태그 작업을 할 수 없다는 이유. 그래서 `index_photos`를 에이전트 도구로 승격했고, 태그가 없으면 검색이 안 되는 문제도 같이 해소됨.
- **도구는 4개로 고정** (`index_photos` · `generate_caption` · `search_photos` · `get_photo`). 사용자가 고친 문장·태그를 저장하는 동작은 앱 기능으로 빼서 Day9 구현량을 줄였다.
- **샘플 사진은 풍경 위주.** 인물·GPS 사진 수집 부담을 피하는 대신, 그 전제에 걸려 있던 guardrail 문항 2건(인물 신원 추정·GPS 경고 우회)을 "사진에 없는 사실 주입 유도"와 "톤 임의 변경 유도"로 교체했다. 카메라 기종 문항(12번)도 같은 이유로 인원수 질의에서 바꿈.
- **커스텀 톤은 프리셋과 글 샘플 둘 다** 지원하되 `data/tones.json` 한 파일에 합쳤다.
- **웹 UI는 PoC 수준**(사진 목록·선택·입력창으로 입출력만 확인). 채점 기준에 UI 항목이 없고 개발 가용 시간이 Day9 8시간 + Day10 오전 3시간뿐이라서.
- **완성 기준은 20건 중 16건(80%) 통과**, guardrail 카테고리는 별도로 전건 통과. 규약이 성공 기준을 숫자로 요구해서 명시.

### 남은 것 · 리스크
- 실제 이미지 10장을 `data/` 에 `photos.json` 의 filename 그대로 넣어야 함. 15번 문항(저화질 추정 태그) 검증용으로 한 장은 일부러 흐릿한 것으로 준비.
- `POST /query` 가 `question` 한 필드만 받는데 UI에서 고른 사진을 어떻게 전달할지 미정. 질문 문자열에 사진 ID를 끼워 넣을지 Day9 구현 시점에 결정.
- 제출 마감 시각이 규약 페이지 안에서 14:00과 15:00으로 엇갈림. 강사에게 확인 필요.
